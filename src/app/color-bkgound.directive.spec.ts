import { ColorBkgoundDirective } from './color-bkgound.directive';

describe('ColorBkgoundDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorBkgoundDirective();
    expect(directive).toBeTruthy();
  });
});
